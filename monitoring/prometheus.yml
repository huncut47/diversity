global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "minitwit"
    dns_sd_configs:
      - names: ["tasks.web"]
        type: "A"
        port: 3000

  - job_name: "node"
    dns_sd_configs:
      - names: ["tasks.node-exporter"]
        type: "A"
        port: 9100
